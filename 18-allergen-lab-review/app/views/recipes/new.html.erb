<h1>Create a New Recipe</h1>

<%= form_for @recipe do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.collection_select :user_id, User.all, :id, :first_name %>
  <%= f.submit %>
<% end %>
